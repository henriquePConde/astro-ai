import type { Theme } from '@mui/material';

export const styles = {
  root: () => (theme: Theme) => ({
    display: 'flex',
    flexDirection: 'column',
    gap: theme.spacing(1.5),
    marginBottom: theme.spacing(1.5),
    width: '100%',
  }),
  item: () => (theme: Theme) => ({
    width: '100%',
    borderRadius: theme.spacing(1),
    backgroundColor: 'rgba(5, 8, 30, 0.78)',
    color: theme.palette.common.white,
    backdropFilter: 'blur(8px)',
    boxShadow: '0 0 18px rgba(0, 0, 0, 0.45)',
    overflow: 'hidden',
  }),
  sectionHeader: () => (theme: Theme) => ({
    width: '100%',
    textAlign: 'left' as const,
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'space-between',
    border: 'none',
    background: 'transparent',
    color: 'rgba(255,255,255,0.9)',
    fontSize: 16,
    fontWeight: 600,
    lineHeight: 1.6,
    cursor: 'pointer',
    padding: theme.spacing(1.25, 1.5),
  }),
  sectionContent: () => (theme: Theme) => ({
    padding: theme.spacing(0, 1.5, 1.5, 1.5),
  }),
  birthTitle: () => (theme: Theme) => ({
    fontSize: 16,
    fontWeight: 600,
    letterSpacing: 0.5,
    lineHeight: 1.6,
    marginBottom: theme.spacing(0.25),
    whiteSpace: 'nowrap',
    overflow: 'hidden',
    textOverflow: 'ellipsis',
  }),
  birthMeta: (color?: string) => (theme: Theme) => ({
    fontSize: 13,
    opacity: 0.85,
    lineHeight: 1.7,
    position: 'relative',
    paddingLeft: theme.spacing(2),
    '&::before': {
      content: '"✦"',
      position: 'absolute',
      left: 0,
      color: color || 'rgba(255, 255, 255, 0.7)',
      fontSize: '0.85em',
    },
  }),
  bigThreeLine: (color?: string) => (theme: Theme) => ({
    fontSize: 13,
    lineHeight: 1.7,
    opacity: 0.9,
    position: 'relative',
    paddingLeft: theme.spacing(2),
    '&::before': {
      content: '"✦"',
      position: 'absolute',
      left: 0,
      color: color || 'rgba(255, 255, 255, 0.7)',
      fontSize: '0.85em',
    },
  }),
  bigThreeButton: () => (theme: Theme) => ({
    marginTop: theme.spacing(0.5),
    padding: theme.spacing(0.5, 1),
    fontSize: 12,
    fontWeight: 600,
    lineHeight: 1.6,
    textTransform: 'none',
    borderRadius: theme.spacing(0.5),
    backgroundColor: 'rgba(255, 255, 255, 0.15)',
    color: theme.palette.common.white,
    border: '1px solid rgba(255, 255, 255, 0.2)',
    cursor: 'pointer',
    transition: 'all 0.2s ease',
    '&:hover': {
      backgroundColor: 'rgba(255, 255, 255, 0.25)',
      borderColor: 'rgba(255, 255, 255, 0.3)',
    },
  }),
  hintTitle: () => (theme: Theme) => ({
    fontSize: 14,
    fontWeight: 600,
    letterSpacing: 0.5,
    lineHeight: 1.6,
    marginBottom: theme.spacing(0.25),
  }),
  hintLine: (color?: string) => (theme: Theme) => ({
    fontSize: 13,
    lineHeight: 1.7,
    display: 'block',
    width: '100%',
    position: 'relative',
    paddingLeft: theme.spacing(2),
    '&::before': {
      content: '"✦"',
      position: 'absolute',
      left: 0,
      color: color || 'rgba(255, 255, 255, 0.7)',
      fontSize: '0.85em',
    },
  }),
  icon: (isOpen: boolean) => ({
    display: 'inline-block',
    transform: isOpen ? 'rotate(90deg)' : 'rotate(0deg)',
    transition: 'transform 0.2s ease',
  }),
  expandButton: () => (theme: Theme) => ({
    marginTop: theme.spacing(0.5),
    padding: theme.spacing(0.5, 1),
    fontSize: 12,
    fontWeight: 600,
    lineHeight: 1.6,
    textTransform: 'none',
    borderRadius: theme.spacing(0.5),
    backgroundColor: 'rgba(255, 255, 255, 0.15)',
    color: theme.palette.common.white,
    border: '1px solid rgba(255, 255, 255, 0.2)',
    cursor: 'pointer',
    transition: 'all 0.2s ease',
    '&:hover': {
      backgroundColor: 'rgba(255, 255, 255, 0.25)',
      borderColor: 'rgba(255, 255, 255, 0.3)',
    },
  }),
};
