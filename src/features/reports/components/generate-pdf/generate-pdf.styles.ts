import { Theme } from '@mui/material';

export const styles = {
  root: () => (theme: Theme) => ({
    display: 'flex',
    alignItems: 'center',
    gap: 2,
  }),
  selectContainer: () => (theme: Theme) => ({}),
};
